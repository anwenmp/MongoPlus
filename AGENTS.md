# MongoPlus Agent Guide

## 项目定位

MongoPlus 是一个 Java MongoDB ORM 框架，目标是让开发者以类似 MyBatis-Plus 的方式操作 MongoDB。

这是一个长期维护的开源项目。开发时优先保证：

1. 公开 API 稳定
2. 向后兼容
3. 模块边界清晰
4. 低侵入、低耦合
5. 源码、测试和文档保持一致

## 新会话启动流程

开始任何任务时：

1. 读取本文件。
2. 如果存在 `docs/ai/INDEX.md`，先读取它。
3. 根据当前任务，只读取 `docs/ai/` 中相关的 1～3 个知识文件。
4. 涉及“如何实现、在哪里、调用链、影响范围”时，优先使用 CodeGraph。
5. 不要为了熟悉项目而遍历整个仓库。
6. CodeGraph 信息不足或可能过期时，再读取相关源码和测试确认。

## 事实优先级

信息冲突时，按照以下优先级判断：

1. 当前源码
2. 当前测试代码
3. Maven POM、自动配置类和配置元数据
4. `docs/ai/` 项目知识库
5. MongoPlusDoc
6. README、注释和历史描述

不得根据类名、旧文档或一般经验猜测功能。

## CodeGraph 使用规则

以下任务优先使用 `codegraph_explore`：

- 查找功能入口
- 理解某个功能如何实现
- 追踪调用链
- 查找接口实现
- 查找方法调用方和被调用方
- 分析修改影响范围
- 定位可能需要更新的测试

查询时尽量同时说明：

- 功能名称
- 已知类或方法
- 希望追踪的起点和终点
- 希望分析的影响范围

不要在 CodeGraph 已经提供准确结果后，再无目的地使用全仓库搜索重复验证。

## 修改前

修改代码前必须：

1. 确定涉及的 Maven 模块。
2. 使用 CodeGraph 定位入口、调用链和影响范围。
3. 阅读相关公开 API。
4. 阅读现有测试和示例。
5. 检查是否涉及兼容性、配置或文档变化。
6. 说明准备修改的文件和原因。

## 修改规则

- 不进行与当前任务无关的重构。
- 不随意改变公开 API。
- 不凭空新增不存在的配置、注解或扩展点。
- 优先复用项目现有工具类、抽象和设计模式。
- 保持现有模块职责和依赖方向。
- 修复缺陷时优先添加回归测试。
- 不因为知识文档中的描述而覆盖当前源码事实。
- 发现知识文档与源码不一致时，以源码为准并修正文档。

## 修改后

完成修改后：

1. 运行受影响模块的测试。
2. 检查编译和兼容性。
3. 检查公开 API、配置项和使用示例。
4. 检查 MongoPlusDoc 是否需要同步。
5. 汇总修改文件、行为变化、测试结果和未验证项。

## AI 项目知识库

长期项目知识存放在：

- `docs/ai/INDEX.md`：任务与知识文件导航
- `docs/ai/PROJECT.md`：项目定位和设计原则
- `docs/ai/MODULES.md`：Maven 模块职责
- `docs/ai/ARCHITECTURE.md`：全局架构
- `docs/ai/CONVENTIONS.md`：开发和 API 约定
- `docs/ai/TESTING.md`：构建与测试策略
- `docs/ai/COMPATIBILITY.md`：JDK、MongoDB Driver 和框架兼容策略
- `docs/ai/DECISIONS.md`：重要设计决策
- `docs/ai/CURRENT_STATE.md`：当前版本和正在进行的工作
- `docs/ai/architecture/`：核心执行链路
- `docs/ai/features/`：功能实现说明
- `docs/ai/integrations/`：Spring Boot、Solon、MongoDB Driver 等集成

不要默认读取全部知识文件，必须通过 `INDEX.md` 按任务加载。

## 知识维护规则

只有发生以下变化时才更新 `docs/ai/`：

- Maven 模块新增、删除或职责变化
- 核心执行流程变化
- 公开 API、注解或配置变化
- 扩展机制变化
- 兼容性策略变化
- 重要架构决策变化

不要记录：

- 临时调试过程
- 普通任务流水账
- 完整源码复制
- 可以随时通过 CodeGraph 得到的全部类和方法
- 尚未实现的设想

知识文件中的重要结论应尽量注明：

- Maven 模块
- 关键类或接口
- 源码路径
- 相关测试